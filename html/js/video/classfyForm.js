define(function(){
    var list = [ //表单
        {
            title : 'classifyName',
            name : '视频分类',
            placeholder : '请填写视频分类标题',
            type : 'text',
            verify : 'imageTitle'
        }    
    ];

    // function getList(callback){
    //  callback(list);
    // }
    return list;
})